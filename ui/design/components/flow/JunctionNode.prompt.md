A branch/merge junction rendered as a rotated rounded square with its logic label.

```jsx
<JunctionNode type="XOR" />
<JunctionNode type="AND" selected />
```

Colors: XOR coral, AND violet, OR amber.
