A flowchart box. Activities are white cards (centered title + actor, optional sub-process chevron, coral ring when selected); `start`/`end` are tinted pills.

```jsx
<FlowNode kind="start" />
<FlowNode title="کنترل کالای دریافتی" actor="انباردار" hasSub selected />
<FlowNode kind="end" />
```
