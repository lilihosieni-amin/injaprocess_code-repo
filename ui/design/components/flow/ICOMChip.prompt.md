Role-colored soft tag for IDEF0 inputs/controls/outputs/mechanisms.

```jsx
<ICOMChip role="input">فاکتور خرید</ICOMChip>
<ICOMChip role="mechanism">صندوق‌دار</ICOMChip>
```

Roles: `input` (blue), `control` (amber), `output` (green), `mechanism` (violet). 7px radius.
