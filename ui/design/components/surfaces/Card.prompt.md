Base surface for panels and content blocks: white, warm 1px border, soft violet shadow, 16px radius.

```jsx
<Card hover pad={22}>…</Card>
```

Set `hover` for the interactive lift (`translateY(-2px)` + deeper shadow).
