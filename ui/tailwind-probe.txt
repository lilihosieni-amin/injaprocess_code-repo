# Every utility this theme names. Listed here so Tailwind emits all of them and
# the built CSS can be grepped for any class before a component uses it.
# Add a line when you add a theme key; never delete one to "clean up".
bg-bg bg-card bg-ink bg-violet bg-coral bg-green bg-conflict bg-muted bg-faint
bg-warm bg-line bg-tile-v bg-tile-v2 bg-tile-c bg-tile-ok bg-tile-warn
bg-tile-dead bg-login-bg bg-login-orb bg-scrim bg-warn bg-info bg-tile-info
bg-icom-input bg-icom-control bg-icom-output bg-icom-mech
bg-violet-mid bg-violet-edge bg-violet-on-dark bg-violet-on-dark-body
bg-violet-on-violet bg-desk bg-tile-v3 bg-tile-v4 bg-tile-c2 bg-tile-ctl
bg-value-current bg-hair bg-line-soft bg-line-dashed bg-border-danger
bg-border-dead bg-border-current bg-border-ok bg-strong bg-body-ink bg-ghost
bg-dialog-ghost bg-ink-current bg-ink-proposed bg-on-dark bg-disabled bg-ok
bg-danger bg-warn-soft bg-info-soft bg-ok-soft bg-danger-soft bg-toast-check
bg-junction-xor bg-junction-and bg-junction-or bg-dept-numeral-violet
bg-dept-numeral-coral bg-steps-sub bg-steps-sub-border bg-steps-sub-hover
bg-steps-group bg-steps-group-border bg-link bg-link-hover
text-violet-on-dark text-violet-on-dark-body text-violet-on-violet text-strong
text-body-ink text-ghost text-dialog-ghost text-ink-current text-ink-proposed
text-on-dark text-disabled text-ok text-danger text-toast-check text-link
text-link-hover text-icom-input text-icom-control text-icom-output text-icom-mech
border-warm border-line border-line-soft border-line-dashed border-border-danger
border-border-dead border-border-current border-border-ok border-hair
border-steps-sub-border border-steps-group-border border-coral border-violet
# Two names, two tokens: `border-border-card` is the hairline `--border-card`
# (the fifth of the --border-* family), `border-card` is the white `--card`
# surface, drawn as a cut-out ring round a badge that overlaps something.
border-border-card border-card
# Task 23's mint (owner ruling, 2026-09-01) — the fact detail's outputs card
# head, which --tile-ok / --border-ok flatten into the unit pill below it.
bg-tile-ok2 border-border-ok2
# Task 3's colours. `--border-pick` is the sixth of the --border-* family and
# takes the family's long form, so `border-border-pick`; the three --line-*
# tokens get both forms, as --line-soft and --line-dashed already do.
bg-tile-v5 bg-surface-sub bg-disc-coral bg-disc-violet
bg-line-divider border-line-divider bg-line-row border-line-row
bg-line-filter border-line-filter bg-border-pick border-border-pick
text-fs-display text-fs-h1 text-fs-h2 text-fs-h3 text-fs-h4 text-fs-h5
text-fs-lg text-fs-body text-fs-sm text-fs-sm2 text-fs-xs text-fs-xxs
text-fs-micro text-fs-doc-base text-fs-doc-h1 text-fs-doc-title
text-fs-doc-step text-fs-doc-body
text-fs-dialog text-fs-menu text-fs-caption
text-fs-numeral text-fs-stat text-fs-steps-title text-fs-display-hand
text-fs-stat-sm text-fs-body-lead text-fs-nano text-fs-badge-sm text-fs-tag
text-fs-h1-reader-home text-fs-h1-reader-list text-fs-h1-reader-dept
text-fs-body-reader
text-body text-caption text-subtitle text-title text-prose
# R3 — the four type roles that change with the surface.
text-role-body text-role-dense text-role-title text-role-hero
font-sans font-mono
font-regular font-semibold font-bold font-extrabold
leading-tight leading-snug leading-normal leading-relaxed leading-loose
leading-looser leading-sub
tracking-eyebrow tracking-display
rounded-badge rounded-chip rounded-control rounded-card rounded-doc
rounded-panel rounded-button rounded-tool rounded-input rounded-search
rounded-tile rounded-feature rounded-pill rounded-round
shadow-card shadow-card-hover shadow-coral shadow-violet shadow-green
shadow-modal shadow-pop shadow-sheet shadow-drawer shadow-card-dark
shadow-stat-dark shadow-guide-hover shadow-ring-flash shadow-conflict-dot
shadow-fab
p-screen-x p-screen-y p-topbar p-half px-screen-x py-screen-y gap-topbar
px-reader-x pb-reader-bottom pt-departments-top pb-departments-bottom
p-s1 p-s2 p-s3 p-s4 p-s5 p-s6 p-s7 p-s8 p-s9 p-s10 p-s11 p-s12 p-s14 p-s16
w-tile h-tile w-tool h-tool w-avatar h-avatar w-logo-bar h-logo-bar
w-logo-login h-logo-login w-touch h-touch min-h-touch min-w-touch min-w-menu
w-tile-reader h-tile-reader w-glyph h-glyph w-glyph-reader h-glyph-reader
w-iconbtn h-iconbtn w-iconbtn-reader h-iconbtn-reader w-fab h-fab
w-fab-reader h-fab-reader w-tick h-tick w-tick-field h-tick-field
w-tick-scope h-tick-scope w-tick-nested h-tick-nested
w-close h-close
max-w-departments max-w-list max-w-summary max-w-doc max-w-drawer
max-w-reader max-w-profile max-w-steps max-w-access max-w-audit
max-w-dialog-wide max-w-dialog-lg max-w-dialog max-w-dialog-sm max-w-dialog-xs
# Logical, not physical: the three --inset-search-icon* tokens are on the
# `inset` scale so the search icon is placed with inset-inline-start and RTL
# stays structural.
start-search-icon start-search-icon-dialog start-search-icon-menu
# §5.2 — the six control paddings, the magnifier's own box, and the empty-state
# card's two-axis 48/20.
p-modal py-search-y px-search-x px-search-x-dialog py-search-y-menu
ps-search-x-menu w-search-glyph h-search-glyph py-empty-y px-empty-x
border-hairline
transition duration-fast duration-chev
-translate-y-lift
backdrop-blur-scrim
# §5.2 — the twelve primitives, minted in one pass so five parallel tasks build
# against a frozen config. Grouped in the order the report's table is:
# TextField · PasswordField · Checkbox · Radio · Dropdown · DataTable · Pager ·
# StatTile · NavTabTray · Timeline · FAB. SectionCard mints nothing — every
# value its two skins draw already had a token and a name.
py-textarea-y p-compose
pe-reveal w-reveal h-reveal w-reveal-glyph h-reveal-glyph rounded-reveal
rounded-tick rounded-tick-nested w-tick-glyph h-tick-glyph
w-tick-glyph-field h-tick-glyph-field
w-tick-glyph-nested h-tick-glyph-nested gap-tick-row py-tick-row-y
py-tick-nested-y
px-radio-x
py-dropdown-y-dialog py-dropdown-y-filter px-dropdown-x-filter p-popover
gap-option py-option-y w-chevron h-chevron max-h-popover
grid-cols-users grid-cols-audit grid-cols-activity py-table-row-y
py-empty-y-inline
w-pager h-pager min-w-page-label
leading-none px-stat-x min-w-stat py-stat-y-grid px-stat-x-grid my-stat-grid
mt-stat-label
py-tab-y-audit min-w-tab gap-tab-flow
py-note-y px-note-x
h-count min-w-count
max1080:hidden max760:hidden
# The single minting pass. The stacking ladder (L-42..L-47), the values the
# Tasks 12-25 sweep found unnamed, the reader's own textarea step, and Task 9's
# mobile row gap.
z-canvas-overlay
z-dropdown z-chrome z-floating z-drawer z-modal z-popover z-tooltip z-toast
ease-css duration-row rounded-bar shadow-feature
max-w-subtitle max-w-intro min-h-chiprow
bg-warn-edge border-warn-edge text-warn-fg
text-role-textarea gap-table-row-mobile
w-menu-more h-menu-more w-login
w-dot h-dot w-chev h-chev w-glyph-tile h-glyph-tile
# §6.0 / §9.7k — the three roles that cannot be written by naming their token:
# the token is a decoy (see tailwind.config.js). `text-on-dark` is NOT the title.
text-role-title-on-field text-role-subtitle-on-field text-role-eyebrow
# §5.2 StatTile — the numeral's gap to its conflict dot. A third 7px role,
# minted rather than borrowed from `gap-stat-label` or `gap-popover` (R8).
gap-stat-dot
# The shell mint. The pass above minted from the SCREENS, so the two shells and
# the summary screen were left writing their own values out; these nine are what
# Tasks 12, 13 and 16 need. `min-w-menu` is NOT here — it is on line 62 already,
# and the mint gave it a token (--width-menu) rather than a new name.
mt-hint leading-lockup min-w-count-chrome h-count-chrome
px-inbox-x py-crumb-y py-back-y px-topbar-reader
grid-cols-idef0
# The reader-chrome mint. The owner's ruling on ReaderShell — the reader takes
# the reader's numbers in all eleven places the plan wrote the panel's — needs
# three names the shell mint above had no reason to look for.
gap-button-icon px-button-x w-menu-more-reader h-menu-more-reader
# The toast mint (Task 25). The design's toast is `padding:12px 20px` (reader
# 978); the 12 is `py-s6` and the 20 had four owners in tokens.css, none of them
# a toast. Both toast components wrote Tailwind's own `px-5` for it.
px-toast-x
# Owner ruling R35 — the drawer-as-bottom-sheet's 22px top corners (§5.2). The
# ladder has no 22 rung; the modal's sheet is 20 and the centred dialog 24.
# Written `rounded-t-sheet` at its one consumer, so the bare stem below is the
# inventory entry and not the spelling any component uses.
rounded-sheet
# The flow-bar mint (owner ruling R47). `src/flow/` was frozen for the whole
# 25-task rebuild (F16), so the flowchart is the one screen that never had a
# conformance pass; these are the nine values R46's audit found with no name at
# all. Two of them had been written down as deferred mints in tokens.css itself
# — the confirm box's padding-y and the ⋯ menu's 225px floor.
py-flowbar-y py-flowbar-y-mobile py-confirm-y py-flowbar-action-y
py-flowback-y px-flowback-x min-w-menu-flow text-crumb-sep w-flowback-mobile
# Owner ruling R48 draws the confirm box those nine left named and unbuilt, and
# the one value it needed that nothing owned: the tick-to-label gap inside it.
gap-confirm
